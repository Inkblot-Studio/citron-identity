# Stage build config (stage.identity.citronos.com) — loaded by vite build.
# Values here are public (baked into the bundle); real secrets never go in this file.
VITE_AUTH_API_URL=https://stage.api.identity.citronos.com
VITE_USE_MOCK_AUTH=false
VITE_ALLOWED_REDIRECT_ORIGINS=https://stage.console.citronos.com
